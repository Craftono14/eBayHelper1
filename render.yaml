services:
  - type: web
    name: ebay-helper
    runtime: node
    buildCommand: npm install && npm run build:backend
    startCommand: npm run migrate && node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
