services:
  - type: web
    name: ebay-helper
    runtime: node
    buildCommand: npm run build
    startCommand: npm run migrate && node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
